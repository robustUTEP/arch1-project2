all:
	(cd timerLib && make install)
	(cd blink_demo && make)
	(cd button_demo && make)
	(cd buzzer_demo && make)
	(cd 1* && make)
	(cd 2* && make)
	(cd 3* && make)
	(cd 4-modular && make)
	(cd 5* && make)
	(cd sw1 && make)
clean:
	(cd timerLib && make clean)
	(cd blink_demo && make clean)
	(cd button_demo && make clean)
	(cd buzzer_demo && make clean)
	(cd 1* && make clean)
	(cd 2* && make clean)
	(cd 3* && make clean)
	(cd 4-modular && make clean)
	(cd 5* && make clean)
	(cd sw1* && make clean)
	rm -rf h lib
